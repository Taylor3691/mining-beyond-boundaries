from core import Visualization, Object
from image import ImageDataset

class ImageDeduplicattion(Visualization):
    def run():
        return
    
    def visitImageDataset(obj: ImageDataset):
        # Task 12, Task 13
        # Thực hiện trình bày thuật toán tìm các ảnh trùng nhau tại đây, sau đó thực hiện xóa các ảnh hoặc kết hợp các ảnh trùng
        # Thực hiện tách thành các hàm nhỏ cho hàm dễ đọc
        # Thực hiện sử dụng bar chart visualize lại số lượng trước và sau khi dedupe
        return
    
    def log(self):
        # Thực hiện in ra màn hình các thông tin xử lý trong giai đoạn này bao gồm
        # 1. Tên bước xử lý (VD: Analysis Histogram Distribution)
        # 2. Tập dữ liệu dữ lý
        # 3. Trạng thái (Success/Failed)
        # 4. In ra thông tin kết quả trả về khi tính toán, linh hoạt trong việc chỉnh sửa hàm
        return